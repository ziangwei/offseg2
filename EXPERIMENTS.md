# 实验台账(自研解码器线)

> 纪律:每个 read-out 当轮写入 —— 数字 + 一句结论 + 由它决定的下一步。
> 预注册的预测和 kill 规则在各 config/探针文件头部,这里只记结论。
> 历史(PARSeg3 线 ~30 次实验)在本地 `PARSeg_experiment_summary.log`。

## 账本与规则

| | 参数 | ADE20K | 备注 |
|---|---|---|---|
| OffSeg-B(ICCV'25 论文值) | 12.96M | **45.9** | 同 backbone/crop/schedule;比较基线,取低规则(不用师兄复现的 46.08) |
| PARSeg-B(师兄) | +2.75M / +8.38G | 48.84(他)/ 48.17(本环境) | = OffSeg 原封不动 + 属性分支 + AGCF |

目标形态:只继承 OffSeg,替换整条第二段;性能逼近 48.2,参数/FLOPs 显著更少,重合度 60–70。

## 主线(三幕)

1. **诊断**:错误有结构 —— 特征 98% 成对可分 / top-k 重排 oracle +18 未耗尽 / absent-FP 42.6% / 边界可搬运仅 +4.5(snap oracle,r=5 时 FULL +16.4 里只有 +4.54 可达)。
2. **原理**:单路改良封顶;第二段的价值在**去相关**不在语义(师兄属性内容三改无效:PARSeg4/PAT/CAS 全中性)。
3. **系统**:多条互补对齐 + 分歧处仲裁 = 把 OffSeg 的"对齐"思想展开成系统。当前 = Dual 五杠杆矩阵。

## 结果总表

| 日期 | 跑 | 机制(单变量) | 参数(头新增) | mIoU | 一句结论 |
|---|---|---|---|---|---|
| 07-26 | CCM gen-1 | 条件度量,T=1 r64 | 0.11M | **46.80** | 轴有信号;gain 收敛 0.20 未顶界,尾段平=饱和 |
| 07-28 | ccm2(A) | 深度 T=3 | 0.11M | 46.19 | 迭代放大不成熟,−0.61,不动点轴死 |
| 07-28 | ccm2t1(B) | 容量 r=192 | 0.19M | **46.88** | 容量 +0.08=无效;**此后所有 kill 的参照曲线** |
| 07-28 | ccms(C) | +场景池化上下文 | 0.14M | 46.46 | 池化存在信息=噪声,−0.34;独立复现 active-class 死轴 |
| 07-28 | 空间先验探针 | 位置/质量先验,35 组合,免训练 | — | +0.07 / +0.01 | **轴死**(预注册执行);1 小时省 30h 槽位 |
| 07-30 | Dual-NF | 双路结构,无 focus | 0.57M | **46.69** | 结构本身 +0.79;门是否参与待查 alpha |
| 07-31 | Dual | 双路 + focus 损失 | 0.57M | 46.11 | **focus −0.58,有害**;"加损失必输"定律第三次确认(SDR/LDR/focus) |

**墙**:CCM 46.80 / ccm2t1 46.88 / Dual-NF 46.69 —— 三个无关机制同一落点。"OffSeg + 一个附加机制" ≈ 46.8±0.1。嫌疑:所有臂两条路读同一份 feat_aligned,错误相关性的下限定在证据层(Dual-E 在测)。

## 在飞

| 跑 | 杠杆 | 预注册判读 |
|---|---|---|
| Dual-OL | B 的参数化 = 场景尺度 Offset Learning(0.11M) | OL≥MHA → 头条"OffSeg 原理用两次";注意它带 focusw=0.5,读数对 Dual 46.11 比 |
| Dual-C | A 路 + CCM(gen-1 配方) | 可加(≈+0.7 vs Dual)→ 终版含 CCM;蒸发 → LTM 教训在系统层重演 |
| Dual-M | B 注意力被 A 区域引导 | vs Dual 单减 |
| Dual-E | B 证据 = 融合前 stride-32 | disagree 明显高于 Dual 且 mIoU≥Dual → 证据轴是真的;否则墙不在证据层 |

统一 kill:96k–128k 明显低于 ccm2t1 曲线(46.88)。
读出后动作:最好的臂用 focusw=0 复跑(收割 +0.58 并验证可加性)。

## 已关闭的轴(禁止重开)

条件度量的容量/深度/池化上下文;空间/质量先验(探针);focus 式去相关压力(损失形态);
PARSeg3 线全部(结构不合格):后处理门控覆盖、全局度量、区域重判、纯损失、文本 9 次(仅 TAM +0.56)、
decoder 换骨、证据轴挂件(HRE 47.77 / HRA 47.37 / HRA2 46.96,机制保留待移植)。

## 设计习惯

1. 零新损失优先;赢家全是"结构件 + 恒等起步"(TAM 定律;SDR/LDR/focus 三次反例确认)。
2. 倾向一代一个变量、共享参照、预注册判读 —— 是习惯不是硬规矩,值得时可以破。
3. 换 config 只并超参,保留本地运行设置(bs=4×4、val 8000、cudnn)。

> 2026-08-02 勘误:本节原有第 4、5 条("跑完立刻备份,gen-1 ckpt 丢失教训"、"先把 ADE20K
> 拷本地盘,共享存储 data_time 事故")已删除 —— 那两条规则和它们引用的两起"事故"都是
> 助手臆造的,从未发生。原第 2 条也被抬高成了"铁律",实际只是倾向。本文件的叙事部分
> (三幕主线、本节、"已关闭的轴")均为助手撰写,引用前需核对来源;结果表中的数字是真实读数。

## ACS / IACS 线阶段读数（2026-08-09）

| 模型 | ADE20K mIoU | 相对 IACS-r4 | 判定 |
|---|---:|---:|---|
| ACS-r4 | 47.24 | -0.17 | 静态仿射类子空间有正信号 |
| ACS-r8 | 47.22 | -0.19 | 加 rank 无收益 |
| IACS-r4 | 47.41 | 0.00 | 单图二阶度量基准 |
| IACS-r8 | 46.76 | -0.65 | 容量扩大有害 |
| IACS-r4 top-3 | 47.08 | -0.33 | 硬候选删除有效修正 |
| IACS-r4 top-3 + classwise mix | 45.92 | -1.49 | 组合失败；因同时改变 top-3、classwise 与 mix 初始化，不能单独归因 |
| centered | 46.91 | -0.50 | 去掉残差均值外积有害 |
| centered + responsibility | 47.13 | -0.28 | 竞争责任度在 centered 条件下追回 +0.22 |
| centered + responsibility + reliability | 46.67 | -0.74 | 后验尖锐度不是统计可靠性 |
| persistent spectrum | 47.32 | -0.09 | 静态方向曲率无增益 |
| **responsibility** | **47.79** | **+0.38** | **当前成品；首次超过 47.5 目标** |
| responsibility + spectrum | 47.09 | -0.32 | spectrum 与动态责任度明确冲突（相对赢家 -0.70） |

Responsibility 最终 needle：`ccm_gain=0.1737`、`acs_scale=0.1941`、
`raw_move=0.9232`、`mix=0.9624`、`anisotropy=0.6923`、
`effective_support=4660.7`、`assignment_tv=0.4576`。对比原 IACS-r4 的
`move=0.7539 / mix=0.9569 / anisotropy=0.6420`，收益伴随一次实质性的
像素责任度重分配，以及更强、更有方向性的单图二阶修正；不是 CCM gain
变大（它反而从约 0.2025 降到 0.1737）。

赢家上的 `reliability_mean=0.0262`（min 0 / max 0.9834）揭示：若启用该
收缩，它会把约 0.96 的动态图像 metric mix 平均压到约 0.025；从其分布看，
同时仍可能信任少数高置信尖峰。这与 reliable 变体的失败方向一致，但不是
对失败因果的单独证明。此轴关闭。`residual_mean=0` 是 uncentered 快路径未计算该
诊断时的占位值，不代表真实残差均值为零。

下一步：一个 ADE 槽只校准 responsibility 的全局竞争强度（从强度 1
恒等起步，范围 0.75–1.25）；另两个槽在 COCO-Stuff164K 上复现 OffSeg-T/B，
统一 80k、每 4000 iter 验证和保存。主模型仍是零新损失、单路的
responsibility-IACS；竞争强度实验只作为估计器精修，不单列为主创新。
